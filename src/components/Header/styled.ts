import { css } from "@emotion/react";

export const Container = css`
  height: 56px;
  background-color: #F2D7D5;
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
`;

export const TopLink = css`
  font-weight: bold;
  display: flex;
  height: 100%;
  align-items: center;
  div {
    padding: 0 20px;
    font-size: 24px;
    color: #F25F5C;
  }
`;
